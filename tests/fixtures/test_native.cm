// test_native.curium - Test native API features

require("public_html/");

// Native str type (alias for string)
str name = input();
name.upper();

// Native malloc/free
ptr data = malloc(1024);
gc_collect();
free(data);

// Print and GC
print("Hello, ");
print(name);
print("!\n");
gc_collect();

// HTTP server
c {
    curium_app_get("/", curium_serve_index);
    curium_app_listen(8080);
}
